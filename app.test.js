const getBooks = require('./getBooks');
const getFinalList = require('./getFinalList');
const addBookToList = require('./addBookToList');


test('adds 1 + 2 to equal 3', () => {
    expect(sum(1, 2)).toBe(3);
});